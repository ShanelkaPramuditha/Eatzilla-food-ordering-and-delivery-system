apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-service
  namespace: eatzilla
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-service
  template:
    metadata:
      labels:
        app: restaurant-service
    spec:
      containers:
        - name: restaurant-service
          image: ${YOUR_REGISTRY}/eatzilla-restaurant-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: eatzilla-config
          resources:
            limits:
              cpu: "300m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: eatzilla
spec:
  selector:
    app: restaurant-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP
