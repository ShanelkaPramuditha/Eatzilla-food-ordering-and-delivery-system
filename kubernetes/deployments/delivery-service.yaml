apiVersion: apps/v1
kind: Deployment
metadata:
  name: delivery-service
  namespace: eatzilla
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delivery-service
  template:
    metadata:
      labels:
        app: delivery-service
    spec:
      containers:
        - name: delivery-service
          image: ${YOUR_REGISTRY}/eatzilla-delivery-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3005
          envFrom:
            - configMapRef:
                name: eatzilla-config
          resources:
            limits:
              cpu: "300m"
              memory: "256Mi"
            requests:
              cpu: "100m"
              memory: "128Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: delivery-service
  namespace: eatzilla
spec:
  selector:
    app: delivery-service
  ports:
    - port: 3005
      targetPort: 3005
  type: ClusterIP
